{% extends 'base1.html.twig' %}
{% block body %}
<h1>Add Lieu </h1>
{{ form_start(form, {attr: {'novalidate': 'novalidate'}}) }}
{{ form_end(form) }}
    <form method="post" action="{{ path('aff') }}" style="padding-left: 80px">
        <input type="submit" value="Liste Siége Hotel" class="btn btn-success">
    </form>
{% endblock %}